"""SourceOS helper tools.

This directory is intentionally lightweight: scripts are designed to run standalone.

We include an empty package marker so scripts can import each other without
copy/paste duplication (e.g., the Truth Plane tick orchestrator).
"""
